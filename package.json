{
  "name": "drizzle-bigint",
  "scripts": {
    "db:seed": "bun run ./db/seed.ts",
    "db:push": "bun drizzle-kit push:sqlite",
    "db:studio": "bun drizzle-kit studio"
  },
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest",
    "drizzle-kit": "^0.20.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@libsql/client": "^0.3.6",
    "drizzle-orm": "^0.29.0"
  }
}